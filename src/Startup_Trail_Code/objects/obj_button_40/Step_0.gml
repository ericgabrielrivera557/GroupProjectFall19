/// @description Finds if mousing is hovering over text
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if (obj_button_parent.menu_index == 0)
{
	obj_button_40.hovering = true;
}